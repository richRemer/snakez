Classic snake game built in Zig and SDL.  This is a small project I've thrown
together to figure out how best to use SDL with Zig.

To build project:

```sh
zig build
```

To run project binary:

```sh
./zig-out/bin/Snakez
```